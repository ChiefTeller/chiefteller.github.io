import './globals.css';
export const metadata = { title: 'Gen1=0', description: 'זוגא בראשית — קיום, מרחב, תנועה והמדינה האחודה', alternates: { canonical: 'https://www.gen1.co.il/' } };
export default function RootLayout({children}: {children: React.ReactNode}) {return <html lang="he" dir="rtl"><body>{children}</body></html>}
